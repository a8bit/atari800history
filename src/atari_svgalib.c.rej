***************
*** 246,255 ****
  	    return AKEY_UI;
  	    break;
  	  case SCANCODE_F5:
  	    return SHIFT_KEY ? AKEY_COLDSTART : AKEY_WARMSTART;
  	    break;
  
  #define KBSCAN(name) \
  	  case SCANCODE_##name: \
  	    keycode |= (AKEY_##name & ~(AKEY_CTRL | AKEY_SHFT)); \
  	    break;
--- 248,265 ----
  	    return AKEY_UI;
  	    break;
  	  case SCANCODE_F5:
  	    return SHIFT_KEY ? AKEY_COLDSTART : AKEY_WARMSTART;
  	    break;
+ 	  case SCANCODE_F11:
+ 	    for(i = 0; i < 4; i++)
+ 	    {
+ 	      if (++TRIG_auto[i] > 2)
+ 		TRIG_auto[i] = 0;
+ 	    }
+ 	    kbcode = 0;
+ 	    break;
  
  #define KBSCAN(name) \
  	  case SCANCODE_##name: \
  	    keycode |= (AKEY_##name & ~(AKEY_CTRL | AKEY_SHFT)); \
  	    break;
